App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =Set(User, LookUp(Users, Office365 = Text(User().EntraObjectId)));
        Set(UserID, Text(User().EntraObjectId);
        'Home Screen';
        
        
         
    StartScreen: ='Home Screen'
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

